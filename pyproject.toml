[project]
name = "kindle-to-anki"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "google-genai==1.73.1",
    "pytest==9.0.3",
    "pytest-mock==3.15.1",
    "python-dotenv>=1.2.2",
]

[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"
